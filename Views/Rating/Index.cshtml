@model IEnumerable<EnterpriseWeb.Models.Rating>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Idea)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdeaUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RatingUp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RatingDown)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubmitionDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Idea.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdeaUser.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RatingUp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RatingDown)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubmitionDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
